Создаем цикл for с переменной i, записываются значения от 1 до 5
В цикле 
  -  создаем переменную file_name куда записываются значение i, например file2.txt
  -  проверяем если файл существует, то другой i берётся и снова проверяется,
     если нет такого файла - то создаётся



